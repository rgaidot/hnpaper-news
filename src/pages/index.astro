---
import { getCollection } from "astro:content";
import TTSPlayer from "../components/TTSPlayer.astro";
import AudioPlayer from "../components/AudioPlayer.astro";
import Layout from "../layouts/Layout.astro";
import { formatFrenchDate } from "../utils/formatDate";
import { getReadingTimeMinutes } from "../utils/readingTime";
import { hasLocalAudio } from "../utils/audio";
import { getLatestNews, sortNewsByDateDesc } from "../utils/news";

const allNews = await getCollection("news");
const sortedNews = sortNewsByDateDesc(allNews);
const latestPost = getLatestNews(sortedNews);

let Content, formattedDate, readingTime, audioFileExists;

if (latestPost) {
  const rendered = await latestPost.render();
  Content = rendered.Content;

  formattedDate = formatFrenchDate(latestPost.data.date);

  readingTime = getReadingTimeMinutes(latestPost.body);

  audioFileExists = hasLocalAudio(latestPost.slug);
}
---

<Layout title="The HNPaper Daily">
  {
    latestPost ? (
      <>
        <article class="mx-auto max-w-5xl">
          <header
            class="mb-10 text-center"
          >
            <div
              class="flex flex-col items-center space-y-2 text-stone-600 font-sans text-[11px] uppercase tracking-[0.25em] pb-8"
            >
              <span class="text-2xl font-bold text-stone-700 uppercase tracking-wider" data-pagefind-meta="title">{formattedDate}</span>
            </div>
            <div class="border-t border-b border-stone-300">
              <TTSPlayer
                title={latestPost.data.title}
                readingTime={readingTime}
                slug={latestPost.slug}
              />
            </div>
          </header>
          <div class="prose prose-stone prose-lg max-w-none mx-auto text-justify bento-grid prose-headings:font-bold prose-headings:text-stone-900 prose-p:font-serif prose-p:text-stone-800 prose-a:text-stone-900 prose-a:underline prose-a:decoration-1 hover:prose-a:decoration-2 prose-a:font-normal prose-strong:text-base prose-blockquote:border-l-2 prose-blockquote:border-stone-900 prose-blockquote:pl-4 prose-blockquote:italic prose-blockquote:text-stone-700 prose-img:border prose-img:border-stone-900 prose-img:p-1">
            <Content />
          </div>

          <script>
            import { initBentoSections } from "../scripts/client/bento";

            initBentoSections();
          </script>
        </article>
      </>
    ) : (
      <div class="text-center py-20">
        <p>Aucune actualité disponible pour le moment.</p>
      </div>
    )
  }
  {latestPost && audioFileExists && <AudioPlayer title={latestPost.data.title} slug={latestPost.slug} formattedDate={formattedDate} />}
</Layout>
